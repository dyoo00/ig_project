import the ig_fetch_functions
use the ig_runner file to execute script

test everything out & let me know if any issues--havent actually tested the ig_runner_file script.
should be enough context/info to get an intuition on logic, etc. though.
 
created a dev branch, so push any changes to that branch